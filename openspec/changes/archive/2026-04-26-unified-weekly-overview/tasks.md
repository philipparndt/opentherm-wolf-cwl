## 1. Implementation

- [x] 1.1 Merge volume into `WeekTimeline` rows — compute per-day total, show right of the bar, warn on >4h gap
- [x] 1.2 Remove `DailyVolume` component and its usage in `SchedulesTab`
- [x] 1.3 Increase `.container` max-width to 900px, add `.tl-vol` + `.tl-warn` CSS, adjust axis margin
- [x] 1.4 Build verification — SUCCESS (30.8KB JS, down from 31.8KB after removing DailyVolume)
