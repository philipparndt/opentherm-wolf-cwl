## 1. Implementation

- [x] 1.1 Update `WeekTimeline` component — accept `airflow` prop, custom tooltip with time range, level, duration, and volume (replaces native `title`)
- [x] 1.2 Add tooltip CSS in `style.css` — fixed position, dark theme, follows cursor
- [x] 1.3 Pass `airflow` to `WeekTimeline` in `SchedulesTab`
- [x] 1.4 Build verification — SUCCESS

## Bonus

- [x] Sort grouped schedule list by start time
