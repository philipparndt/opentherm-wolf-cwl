## 1. Implementation

- [x] 1.1 Update `daysStr()` in web UI `App.tsx` — return "Every day", "Weekdays", "Weekend", or individual days
- [x] 1.2 Update `getActiveScheduleDescription()` in `scheduler.cpp` — same grouping logic for OLED/MQTT
- [x] 1.3 Build verification — SUCCESS
