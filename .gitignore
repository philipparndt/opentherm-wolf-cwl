### macOS
# Finder metadata
.DS_Store

# Thumbnails
._*

# Custom folder icons
Icon

# Volume root files
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

### Go
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work.sum

# env file
.env
*.sal
*.csv
!esp32/partitions.csv
decode/decode
esp32/target
esp32/.embuild
esp32/data
esp32/web/node_modules
esp32-diyless/target
esp32-diyless/.embuild
esp32-diyless/data
esp32-diyless/web/node_modules

# PCB — shared
pcb/.venv
pcb/.venv-3d
pcb/__pycache__
pcb/fp-info-cache
pcb/.history

# PCB — KiCad local/generated (all variants)
pcb/**/*.kicad_prl
pcb/**/*.kicad_pcb-bak
pcb/**/*.kicad_sch-bak
pcb/**/*.net
pcb/**/*.xml
pcb/**/*.svg
pcb/**/*.erc
pcb/**/*.drc
pcb/**/*.step
pcb/**/*.stl

# PCB — per-variant output / local dirs
# (pcba/ kept tracked so ordered-parts.md can be committed; *.csv and *.zip
# inside it are caught by the global rules above.)
pcb/*/gerber/
pcb/*/plot/
pcb/*/.history/
pcb/*/cwl-backups/
pcb/*/*-backups/
pcb/*/cwl-gerber.zip

wolf-cwl-core/target
*.lck
*.log
*.3mf
*.zip
.idea
out
